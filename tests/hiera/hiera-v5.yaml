---
version: 5
hierarchy:
  - name: "hiera config for unit test"
    data_hash: yaml_data
    datadir: .
    paths:
      - "%{::fqdn}.yaml"
      - "%{::environment}.yaml"
      - common.yaml
  - name: "Second hierarchy not used for now"
    paths:
      - common.yaml
